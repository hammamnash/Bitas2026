{
  "name": "bitas2026",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build-pages.mjs",
    "test": "node --test tests/build-pages.test.mjs",
    "dev": "npm run build && sirv public --host 127.0.0.1 --port 8123 --dev"
  },
  "devDependencies": {
    "htmlparser2": "12.0.0",
    "sirv-cli": "3.0.1"
  }
}
